DB_URL="postgresql://username:password@localhost:5432/katana_id"
PORT="8080"
RESEND_API_KEY="re_9oqu4mN1_AaaaaAaAAAAaAaaaAaaaaA"
ALLOWED_ORIGINS="https://katanaid.com,https://www.katanaid.com"

# Base URL of this server; must exactly match the redirect URI registered
# with each OAuth provider (SERVER_URL + /auth/google/callback, etc.)
SERVER_URL="https://api.katanaid.com"
# Base URL of the frontend; OAuth flows redirect here after sign-in
FRONTEND_URL="https://katanaid.com"

# Google OAuth (console.cloud.google.com > APIs & Services > Credentials)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# GitHub OAuth (github.com/settings/developers)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""